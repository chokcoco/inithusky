/**
 * commitlint 规则配置文件，由 inithusky 自动产出
 * 默认采用 commitlint 提供的默认包，各团队可根据实际情况自行修改扩充规则
 * https://github.com/conventional-changelog/commitlint
 */
module.exports = { 
  extends: ['@commitlint/config-conventional'],
};